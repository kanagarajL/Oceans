package com.props.ocean.repository;

import com.props.ocean.model.Prop;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PropRepository extends JpaRepository<Prop,Integer> {
}
